import styled from 'styled-components';
import ThreadMenuItem from './ThreadMenuItem.styled';

const SalesMarketingThread = styled(ThreadMenuItem)`
    top: 80px;
`;

export default SalesMarketingThread;